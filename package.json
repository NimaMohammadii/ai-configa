{
  "name": "ai-configa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "set:webhook": "node scripts/set-webhook.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
