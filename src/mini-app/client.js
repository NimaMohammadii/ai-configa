export const MINI_APP_JS = `const tg=window.Telegram?.WebApp;tg?.ready();tg?.expand();const $=(id)=>document.getElementById(id);const text=$("ttsText"),count=$("charCount"),button=$("convertButton"),status=$("status"),player=$("audioPlayer"),voice=$("voiceName"),balance=$("balance");const initData=tg?.initData||"";text.addEventListener("input",()=>{count.textContent=Array.from(text.value).length});async function api(path,body){const res=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({initData,...body})});const data=await res.json().catch(()=>({error:"پاسخ نامعتبر دریافت شد."}));if(!res.ok)throw new Error(data.error||"درخواست انجام نشد.");return data}async function load(){try{const data=await api("/mini-app/api/session",{});voice.textContent=data.voice||"Nora";balance.textContent=Number(data.balance||0).toLocaleString("en-US")}catch(e){status.textContent=e.message;status.classList.add("error")}}button.addEventListener("click",async()=>{const value=text.value.trim();if(!value){status.textContent="لطفاً ابتدا متن را وارد کنید.";return}button.disabled=true;status.classList.remove("error");status.textContent="در حال ساخت صدا...";player.hidden=true;try{const data=await api("/mini-app/api/tts",{text:value});voice.textContent=data.voice;balance.textContent=Number(data.balance).toLocaleString("en-US");player.src="data:audio/mpeg;base64,"+data.audioBase64;player.hidden=false;await player.play().catch(()=>{});status.textContent="آماده است."}catch(e){status.textContent=e.message;status.classList.add("error")}finally{button.disabled=false}});load();`;
