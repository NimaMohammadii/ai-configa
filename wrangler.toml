name = "ai-configa"
main = "src/worker.js"
compatibility_date = "2026-05-28"
workers_dev = true
preview_urls = true

# Secrets are not written here. Set them with:
# npx wrangler secret put BOT_TOKEN
# npx wrangler secret put ELEVEN_API

# D1 is required for persistent user state and demo cache.
# See wrangler.toml.example and README.md for the DB binding block.
